package com.ownimage.framework.util;

import java.util.logging.Logger;

import com.ownimage.framework.logging.StringBufferHandler;

/**
 * The class Lock return an object that can be used as a semaphore.
 */
public class Lock {

	public final static Version mVersion = new Version(4, 0, 0, "2014/05/06 20:48");
	private final static Logger mLogger = Logger.getLogger(StringBufferHandler.class.getName());
}
